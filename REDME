```HTML
<div id="mapid1">
</div>
```

```Javascript
var options = {
  tileLayer : "http://{s}.tile.osm.org/{z}/{x}/{y}.png",
  };
  
var cmap = new MapCola("mapid1");

cmap.AddLayer("url");
cmap.AddLayer("url");
```
